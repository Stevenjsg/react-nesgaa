
import { createMuiTheme } from "@material-ui/core/styles";

let theme = createMuiTheme({
    palette: {
        primary: {
            light: '#DFDCEF',
            main: '#FBFBFB'
        },
        secondary: {
            light: '#009063',
            main: '#3B3B3B'
        }
    }
})
export default theme;