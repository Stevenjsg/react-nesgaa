
import { createMuiTheme } from "@material-ui/core/styles";

let theme = createMuiTheme({
    palette: {
        primary: {
            dark: '#8E8E8E',
            main: '#FBFBFB'
        },
        secondary: {
            light: '#DEDEDE',
            main: '#CECECE'
        }
    }
})
export default theme;